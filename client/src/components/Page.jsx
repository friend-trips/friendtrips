import React, {useState, useEffect} from 'react';

const Page = ({color}) => {
  return (
    <div style={{flexGrow: 1, backgroundColor: color, border: 1, height: '100%'}}>

    </div>
  );
};

export default Page;